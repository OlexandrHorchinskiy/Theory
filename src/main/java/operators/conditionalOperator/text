Умовні оператори (Conditional operators):
if  - "якщо, то ..."
if else  - "якщо, то ... інакше, то ..."
if else if - "якщо, то ... інакше, якщо то ..."
if else if else - "якщо, то ... інакше, якщо то ... інакше, то ..."
змінна = умова ? результат якщо true : результат якщо false