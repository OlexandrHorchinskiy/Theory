package enum_;

public enum DogType {
    // Перелік. Перелік типів для об'єктів. Зручно використовувати коли перелік обмежений.
    Dalmatian,      // Кожна стрічка в enum це об'єкт класу enum.
    Kokker,
    Sharik
}