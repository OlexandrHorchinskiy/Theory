Java Collections Framework
https://docs.oracle.com/javase/8/docs/technotes/guides/collections/
https://habr.com/ru/post/237043/ - Справочник по Java Collections Framework
https://proft.me/2017/04/10/pro-java-collections-framework-pochti-vse-i-srazu/ - Про Java Collections Framework: почти все и сразу

Java Collections Framework - это коллекция интерфейсов и классов, которые используются для сохранения и обработки данных.
Коллекции - это хранилища или контейнеры, поддерживающие различные способы накопления и упорядочения объектов с целью
обеспечения возможностей эффективного доступа к ним.

Они представляют собой реализацию структур данных, поддерживающих три основные операции:
•	добавление нового элемента в коллекцию;
•	изменение элемента в коллекции;
•	удаление элемента из коллекции.

Collection расширяет интерфейсы:
•	List – хранит упорядоченные элементы (элементы могут дублироватся).
    Имеет реализации:
    o	ArrayList - его преимущество в скорости навигации по коллекции.
    o	LinkedList - его преимущество в скорости вставки и удаления элементов в коллекции.
    o	Vector - синхронизирован(?), и поэтому в одном потоке, он работает медленней остальных реализаций.

•	Set – не содержит повторяющихся элементов.
    Основные реализации:
    o	TreeSet - упорядочивает элементы по их значениям.
    o	HashSet - упорядочивает элементы по их хэш ключам (может показаться что элементы хранятся в случайном порядке).
    o	LinkedHashSet - хранит элементы в порядке их добавления(?).

•	Queue - используется для реализации очереди в Java. Очереди работают по принципу FIFO (First In First Out).
    Основные реализации:
    o	LinkedList
    o	PriorityQueue

•	Map - интерфейс для реализации карты, где элементы хранятся с их ключами. Ключи в Map не могут быть одинаковыми!
    Основные реализации:
    o	HashTable - синхронизирована, объявлена устаревшей(?).
    o	HashMap - порядок(?) элементов рассчитывается по хэш ключу.
    o	TreeMap - элементы хранятся в отсортированном(?) порядке.
    o	LinkedHashMap - элементы хранятся в порядке вставки(?).